grades = []
print(grades)

for index in range(0, 3):
    
    # the user is going to enter his grade
    grade = input("Enter a grade: ")
    
    # append to add the grade to the array (list)
    grades.append(int(grade))
    
print(grades)